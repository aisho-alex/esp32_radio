/*
  Placeholder for the test-only env:native: "pio run" requires at least one
  buildable source. This file is excluded from all firmware builds via
  build_src_filter and never runs on a device.
*/
int main() {
  return 0;
}
